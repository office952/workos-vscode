# WORKOS — Intake V6 Golden Reference Modular Reproduction Alignment

**Date:** 2026-07-17  
**Mode:** Plan realignment · read-only research  
**No implementation · No application edits · No DB write · No seed · No migration · No commit**

**Primary verdict:** `GOLDEN_REFERENCE_ALIGNMENT_READY`

**Not:** `IMPLEMENTATION_READY`

**Binding addendum:** SVG Analyzer + real UI golden flow (CorelDRAW → nest2 → Intake) is part of this alignment — not optional geometry trivia.

---

## 1. Verdict

| Item | Value |
|------|--------|
| Verdict | `GOLDEN_REFERENCE_ALIGNMENT_READY` |
| SVG Analyzer role | Detection + measurement + proposed interpretation — **not** commercial/sold-scope authority |
| Golden SVG UI proof | **PROVEN_SAME_DAY** via disposable WS evidence (see §SVG-14); fresh re-script this turn tooling-blocked in plan-mode |
| Prior modular audit | Valid leakage inventory — **subordinate** to parity-first route |
| Prior Gates A–D pack | Valid findings — **subordinate** to active modular scope after parity |
| Implementation GO | **Not issued** |

---

## 2. Mini decision

```text
INTAKE V6 ACTUAL = GOLDEN REFERENCE (keep behavior)
PRODUCT SYSTEM   = modular contract source
INTAKE V6 FINAL  = dynamic reproduction of golden reference from contracts
SVG (Corel export) = GOLDEN TECHNICAL INPUT
SVG ANALYZER     = detect + measure + propose (operator confirms)
```

Order is binding:

1. **Full-product parity** — reproduce `TPL-VOLUMETRIC-LETTERS_v2` 1:1 including **same SVG facts, layer IDs, colors, geometry, operator approvals**, formulas, quantities, materials, ops, readiness, CPP, Review, PD, Aggregate.
2. **Modular flexibility** — same spine for valid subsets; SVG facts consumed **selectively** per active components (cant-only uses perimeter, not face material, etc.).

Hardcoding is an **ownership extraction** problem, not a functional bug, when golden output is correct.  
Modularization ≠ UI hide. Inactive outputs must vanish across Intake → PD → Aggregate → CPP → Snapshot → Execution preview.

**Recommended route:** Build 1 extraction+parity harness (incl. SVG parity contract) → Build 2 full-product reproduction from contracts → Build 3 subset isolation → Build 4 freeze/exec proof (separate GO). Prefer large coherent builds; micro-slice only for schema risk.

**Do not** implement SVG Analyzer redesign in early builds — extract and preserve its golden contribution.

---

## 3–6. Repository / runtime / HEAD

| Check | Result |
|-------|--------|
| Repo | `C:/w/psiso` |
| Remote | `https://github.com/office952/workos-vscode.git` |
| Branch | `feature/product-system-active-path-isolation-v1` |
| Start / End HEAD | `163f35f` (unchanged) |
| Frontend | `:3000` UP |
| Backend | `:8001` healthy |
| Golden WS (read-only) | `4888fddb-5d9f-46cb-9bcc-5dd3ed1263b1` / `IV6-C8066690` |
| SVG | `C:/Users/offic/Desktop/fisiere-teste-svg/gradi-curat.svg` |

---

## 7. Dirty-tree protection

This pack docs = new untracked. Historical audits/evidence/WS untouched. Unrelated dirty tree untouched. No reset/stash/`git add .`.

---

## 8. Compound Engineering execution

```text
PARALLEL READ-ONLY RESEARCH
→ SINGLE SYNTHESIS
→ ADVERSARIAL REVIEW
→ ALIGNED TARGET CONTRACT
→ IMPLEMENTATION ROUTE
→ OWNER GATE
→ STOP
```

Agents A–H: golden UI, formulas, pricing, PS ownership, PD/scope, Aggregate/interface, persistence/snapshot, adversarial.

**Added (binding):**
- **SVG-A** — Parser/geometry (layers, groups, IDs, colors, contours, transforms, dims, perimeter, area)
- **SVG-B** — Classification/provenance (LETTER/LOGO/ACM/atypical, confidence, warnings, operator confirm)
- **UI-E2E** — Disposable golden walkthrough (Intake → SVG → layers → components → form → calc → Review → save → PD → Aggregate → CPP)
- **Adversarial SVG** — Lost colors/IDs/hierarchy; false classification; formula≠UI geometry; CPP divergence; hard-refresh loss

---

## 9. Agent reports (compressed)

| Agent | Result |
|-------|--------|
| A Golden UI | Review shell + offer scope + layers = operator golden path; preserve |
| B Formulas | Geometry/LED/adhesive/PSU owned by services; form maps keys |
| C Pricing | CPP 7G + `commercial_rules_volumetric_v2`; Intake ≠ money |
| D Product System | Templates, mini-modules, dossier exist; component template runtime partial |
| E PD / scope | Full-product always_on + subset path; legacy `active=[]` |
| F Aggregate / interface | Bonding ops composition-gated; adhesive misowned on cant |
| G Persistence | Settings travel in finish_setup without rich provenance |
| H Adversarial | Risk = rewrite formulas/prices under “modular”; UI-hide greenwash |

---

## 10. Read-only compliance

No app edits, no DB writes, no seeds, no migrations, no quote/order/snapshot, no commit.

---

## 11. Golden reference definition

**Golden reference** = live Intake V6 operator experience + backend spine for Letters full product that already works E2E (upload → configure → Review → PD/Aggregate → CPP dry-run boundary), exemplified by:

- Template `TPL-VOLUMETRIC-LETTERS_v2`
- Workspace `IV6-C8066690` / `4888fddb-…` (historical fixture, read-only)
- SVG `gradi-curat.svg`

Use it to **understand and extract** contracts — not to declare the system broken.

---

## 12. Golden behavior inventory (classification)

| Behavior | Class |
|----------|--------|
| SVG upload / analyze / persist | `GOLDEN_REFERENCE_KEEP` |
| Save / reload / hydration | `GOLDEN_REFERENCE_KEEP` |
| Layer / group visibility | `GOLDEN_REFERENCE_KEEP` |
| Letters/Logo roles PARTIAL | `GOLDEN_REFERENCE_KEEP` / extract later |
| Face / cant / back configuration | `GOLDEN_REFERENCE_KEEP` |
| Validated technical formulas / quantities | `GOLDEN_REFERENCE_KEEP` |
| Validated commercial path via CPP | `GOLDEN_REFERENCE_KEEP` |
| FINISH ≠ MOUNTING decoupling | `GOLDEN_REFERENCE_KEEP` |
| Șablon ≠ SURFACE_FINISH | `GOLDEN_REFERENCE_KEEP` |
| Review UX as operator reference | `GOLDEN_REFERENCE_KEEP` |
| Full-product happy path | `GOLDEN_REFERENCE_KEEP` |
| PD preview / Aggregate compile | `GOLDEN_REFERENCE_KEEP` |
| Responsive baseline | `GOLDEN_REFERENCE_KEEP` |
| Historical snapshot compatibility | `LEGACY_COMPATIBILITY` |
| Hardcoded full-product field shell | `GOLDEN_REFERENCE_EXTRACT` (not functional bug) |
| Soft defaults illuminated/template | `GOLDEN_REFERENCE_EXTRACT` (+ Gate A/B later) |
| Adhesive owned by cant alone | `REAL_BUG` for **subset** isolation; full-product bonding still correct commercially |
| Montaj UI when sold FACE-only | `GOLDEN_REFERENCE_EXTRACT` / subset LEAKAGE |
| ACM role | `UNPROVEN` / absent — out of scope |

---

## 13. Formula inventory (do not change)

| Quantity | Owner (keep) | Notes |
|----------|--------------|-------|
| Face area / perimeter | `svg_analyzer` + quote geometry services | Form maps keys only |
| Return depth | Operator select + material variant map | Not invent as derived |
| LED counts | `intake_v4_led_lighting_service` + density rules | Pitch / emblem rules frozen |
| PSU | LED service + propose units | Piece commercial line |
| Template area | Operator / document fallback | Not invent in CPP |
| Adhesive qty | `shared_edge_cant_rules` + consumables service | Qty frozen; **ownership** → interface later |

---

## 14. Pricing behavior inventory (do not change)

| Layer | Role |
|-------|------|
| Intake | Truth / config / preview — **no money authority** |
| CPP `commercial_price_proposal_service` | Commercial proposal |
| `commercial_rules_volumetric_v2` | Qty→money rules |
| V6 dry-run / write | Server recompute via CPP |

**Pricing freeze:** modularization must not alter tariffs, rule codes, or qty→money mapping. Exact CPP parity required for golden input.

---

## 15. UI behavior inventory (preserve, then compose)

| Surface | Golden keep | Extract later |
|---------|-------------|---------------|
| Straturi upload + layers + composition | Keep | Geometry/role contracts |
| Configurare Finisaje / Iluminare / Montaj | Keep full-product layout | Compose from module contracts |
| Offer scope subset chips | Keep capability | After parity, drive silence |
| Review blockers / ownership notes | Keep | Category provenance |
| Defaults (lit, template, cant 60) | Keep for full product | Provenance as product_default |

---

## 16. Persistence behavior inventory

| Today | Target |
|-------|--------|
| `finish_setup` flat + letter groups | Same values for parity |
| Weak provenance (no per-field source/owner) | Additive provenance metadata |
| Hide≠purge on subset | After parity Build 3: ignore/purge inactive |

Allowed difference vs golden for parity builds: **provenance additions only**, not value changes.

---

## 17–18. Current vs target ownership map

| Concern | Current owner | Target owner |
|---------|---------------|--------------|
| Operator form chrome | Intake FE hardcoded | Intake renderer from form contracts |
| Geometry facts | SVG analyzer services | Same (capability) |
| Face / cant / back settings | finish_setup flat | Component/mini-module contracts |
| LED/PSU | finish + lighting services | LIGHTING mini-module + services |
| Surface finish | finish_setup / finisaje | SURFACE_FINISH module |
| Șablon | finish_setup | sablon_montaj |
| Structure | mounting_solution | structura_suport |
| Packaging | PD always_on composition | Explicit composition rule (Gate D) |
| Bonding + adhesive | Ops composition-gated; adhesive on cant | **Interface FACE+RETURN** |
| Money | CPP | CPP (unchanged) |
| Technical graph | Aggregate | Aggregate filtered by active scope |
| Freeze | Snapshot services | Frozen modular graph |

---

## 19–21. Product System / component / mini-module contract targets

Reuse existing: product templates, dossier, mini-module registry, form contract backbone, offer_scope map.  
**Avoid** mega-schema and parallel catalog.  
Component template runtime = **partial** today (docs + shared codes) — extend minimally when extraction needs it.

Conceptual contract payload (not a mandatory single table):

```text
identity, version, owner, capabilities,
form_fields, settings, options, defaults, default_provenance,
validations, measurements, formulas, materials, operations,
readiness, commercial_eligibility, snapshot_payload, execution_hints,
dependencies, interface_dependencies, compatibility
```

---

## 22–24. Interface contracts + bonding + adhesive

### FACE + RETURN (mandatory)

| Scope | Bonding | Adhesive |
|-------|--------:|---------:|
| FACE only | NU | NU |
| CANT only | NU | NU |
| FACE + CANT | DA | DA |
| Full product | DA | DA |

| Fact | Value |
|------|--------|
| Material | `MAT-ADEZIV-CANT-LITERE` / `adhesive_return_to_face` |
| Current owner | `modelare_cant` (wrong for subset law) |
| Correct owner | Interface / composition |
| Full-product effect | Emits (golden keep qty) |
| Subset cant-only | Must not emit (after Build 3) |

### Other interfaces (candidates)

| Interface | Requires | Current | Correct |
|-----------|----------|---------|---------|
| LED adhesive modules | LIGHTING + mount path | sistem_led gated | Keep lighting-owned |
| Letter assembly | Multi-component | Parent ops | Composition |
| Structure + mounting | Support solution | mounting_solution | MOUNTING path |
| Template + mounting prep | Template enabled | finish flags | sablon + prep |
| Packaging + delivery | Composition / exec omit | always_on / omit | Gate D |

---

## 25. Settings provenance model (conceptual)

```text
component_instance.settings.<key>:
  value, source (operator|product_default|derived|legacy),
  owner, confirmed, contract_version, consumers[]
```

**Schema:** not required for Build 1 inventory.  
Rich provenance persist → may become `SCHEMA_CHANGE_REQUIRED` later; prefer additive JSON in payload before Alembic.

---

## 26. Active Scope target

| Mode | Role |
|------|------|
| Legacy full (`active=[]`) | Compatibility for old WS/snapshots — **not** new target UX |
| Explicit full_product | Golden parity request mode |
| component_subset | Flexibility after parity |
| Interface deps | Activate by combination with provenance |

---

## 27–31. Downstream impact (parity-preserving)

| Layer | Rule |
|-------|------|
| PD | Same selected technical truth for golden; later filter by scope |
| Aggregate | Same graph for golden; workspace filter for subsets |
| CPP | **Exact commercial parity** on golden; active-only later |
| Snapshot | Equivalent frozen truth; no historical mutation |
| Execution | Equivalent candidates from freeze; no materialization in early builds |

---

## 32. Full-product parity contract

```text
Same golden input
→ same visible fields
→ same persisted values (provenance may add)
→ same ProductDefinition technical truth
→ same ProductAggregate outputs
→ same quantities / formulas
→ same materials / operations
→ same CPP commercial result (exact)
→ same readiness / Review
```

| Layer | Allowed difference |
|-------|-------------------|
| Intake UI | Zero functional difference |
| Payload | Provenance additions only |
| PD / Aggregate | Equivalent truth + provenance |
| CPP | **Exact** |
| Snapshot / Exec preview | Equivalent |

Fixture: disposable clone of golden path with `gradi-curat.svg` — do not mutate `4888fddb-…`.

---

## 33. Modular scenarios (after parity)

| # | Scenario | Class |
|---|----------|-------|
| 1 | FACE only | `SUPPORTED` (Slice1) |
| 2 | CANT only | `SUPPORTED` + adhesive silence required |
| 3 | FACE + CANT | `SUPPORTED_WITH_DEPENDENCY` (interface) |
| 4 | BACK only | `SUPPORTED` |
| 5 | FACE+CANT+BACK | `SUPPORTED` |
| 6 | LIGHTING only | `OWNER_DECISION_REQUIRED` / may be `TECHNICALLY_VALID_NOT_COMMERCIAL` |
| 7 | TEMPLATE only | `OWNER_DECISION_REQUIRED` (MOUNTING deferred) |
| 8 | STRUCTURE only | `OWNER_DECISION_REQUIRED` (deferred) |
| 9–11 | Full non-lit / lit / no mounting | `SUPPORTED` with intent gates |
| 12 | Packaging separate | `OWNER_DECISION_REQUIRED` (Gate D) |

---

## 34. Inactive output silence

Law unchanged: inactive → zero required/missing/warnings/blockers/materials/measurements/ops/CPP/tasks except proven deps with provenance.  
Parity builds must not break full-product by prematurely silencing composition rules that golden relies on.

---

## 35. Gates A–D repositioned

| Gate | After parity question |
|------|----------------------|
| A Illumination | When is LIGHTING in scope? How does full-product default reproduce? How silent without LIGHTING? |
| B Template | When is șablon selected/required? Full-product reproduce? Absent in cant-only? |
| C No extra support | Only if MOUNTING/structure in scope; never `installation_template` proxy |
| D Packaging | Composition vs conditional vs module vs exec-only vs legacy — owner choose; not sold |

---

## 36. Alternatives evaluated

| Alternative | Decision |
|-------------|----------|
| Rewrite Intake as “wrong” | Rejected — golden keep |
| Subset isolation before parity | Rejected — owner order |
| UI hide only | Rejected — adversarial fail |
| Change formulas/prices for modularity | Forbidden |
| Parallel catalog / second Intake | Forbidden |
| Single mega Build all-at-once | Too risky — prefer 1→2→3→4 |
| Extract-only without parity tests | Incomplete |

---

## 37. Recommended architecture

```text
GOLDEN INTAKE V6
→ ownership extraction (contracts)
→ modular contracts in Product System
→ full-product reproduction (parity proof)
→ subset composition + output isolation
→ CPP parity (golden) / active-only (subset)
→ frozen modular snapshot
→ Execution preview
```

Systems: reuse families, templates, mini-modules, form contract, SVG analyzer, Intake renderer, workspace, offer/active/dependency resolvers, PD, Aggregate+filter, readiness, CPP, snapshot, exec preview, governance. Add **Interface Resolver** as thin service or extend active-scope — avoid duplicate catalogs.

---

## 38. Recommended implementation route

| Build | Name | Exit |
|------:|------|------|
| **1** | Golden Reference Contract Extraction | Inventory + ownership + interface specs + **parity harness** (same input→same output) · **no subset UX change** · no formula/price change |
| **2** | Modular Full-Product Reproduction | Intake composed from contracts; UI/logic/PD/Aggregate/CPP **match golden** |
| **3** | Subset Activation + Output Isolation | cant/face/face+cant; adhesive interface; zero inactive outputs; Montaj gated |
| **4** | Downstream Freeze + Execution Proof | Snapshot/exec from frozen modular graph · **separate owner GO** · no materialization |

**Recommendation:** follow **1→2→3→4**. Do not skip 1. Do not start 3 before 2 green. Gates A–D land in 2–3 as scoped by active modules.

---

## 39. File / ownership matrix (primary)

| File/function | Current | Golden | Target owner | Extraction | Risk | Test |
|---------------|---------|--------|--------------|------------|------|------|
| `IntakeV6ReviewStep.tsx` | Full form + defaults | Operator UX | Renderer + contracts | Map fields→modules | High if rewrite | Parity UI |
| `intakeV6SoldScopeVisibility.ts` | Subset hide | Keep | Scope renderer | After B3 | Med | Subset |
| `offer_scope` + resolvers | Slice1 subset | Keep | Active scope | Extend interface | Med | Subset |
| `product_definition_builder_service.py` | always_on + subset | Keep full | PD + contracts | State from contracts | High | Parity PD |
| `product_aggregate_service.py` | Full BOM | Keep full | Aggregate | Prefer workspace filter authority | High | Parity AGG |
| `product_aggregate_active_scope_filter.py` | Subset filter | Keep | Aggregate | Adhesive interface | High | Cant-only |
| `shared_edge_cant_rules` / adhesive services | Qty + cant owner | Keep qty | Interface | Ownership only | High | Adhesive |
| `commercial_price_proposal_service.py` | CPP | Keep | CPP | No tariff edits | Critical | Exact CPP |
| `commercial_rules_volumetric_v2.py` | Money rules | Keep | CPP data | Freeze | Critical | Exact CPP |
| `active_scope_resolver_service.py` | Legacy + subset | Keep | Scope | Interface deps | Med | Scope |
| Snapshot / exec preview services | Freeze/preview | Keep | Downstream | B4 | Med | Compat |
| Dossier / mini-module registry | Contracts | Keep | PS | Enrich | Med | Contract |
| Tests listed in modular audit | Partial modular | Extend | QA | Parity + subset | — | Plan §42 |

No new files without justification; prefer enriching existing contracts.

---

## 40–42. Test / runtime proof / compatibility plans

### Golden parity tests (Build 1–2)
Field, formula, measurement, material, operation, **CPP exact-value**, readiness, persistence, Review parity.

### Modular tests (Build 3)
Cant-only / face-only / face+cant; no adhesive cant-only; adhesive face+cant; silence LED/sablon/mounting/packaging per decisions.

### Compatibility
Legacy WS, old snapshots, `active=[]` passthrough, existing quotes/orders/exec previews — no mutation.

### Runtime proof (future, not now)
- Disposable `/intake-v6/<ws>/operator` + `gradi-curat.svg`
- Same UI/values/formulas/PD/Aggregate/CPP/blockers/hard refresh
- Modular scenarios: click path, payload, modules, materials, ops, CPP, snapshot eligibility
- No quote/order in plan mode

---

## 43. Schema / migration assessment

| Need | Build 1 | Later |
|------|---------|-------|
| Inventory + harness | No schema | — |
| Additive provenance in payload JSON | Prefer | — |
| New interface kind tables | Avoid | Possible `SCHEMA_CHANGE_REQUIRED` |
| Alembic | **NO** unless owner mandates new tables | |

---

## 44. Owner decision table

| Decision | Options | Recommendation | Evidence | Risk | Blocks |
|----------|---------|----------------|----------|------|--------|
| 1 Golden reference status | Rewrite vs keep | **Keep as golden** | Owner law | Low | All |
| 2 Full-product parity first | Yes / subset first | **Yes** | Owner order | High if skipped | B2+ |
| 3 Contract ownership | Mega-schema vs extract into existing | **Extract into existing PS/form** | Systems map | Med | B1 |
| 4 Settings provenance | Flat keep / additive / new tables | **Additive after parity** | Payload | Schema risk | B2–3 |
| 5 Interface model | Ops only / ops+materials | **Ops+materials** | Adhesive | High | B3 |
| 6 Active scope target | Fill active[] / explicit modes | Explicit full+subset; legacy keep | Resolver | Med | B2–3 |
| 7 Legacy full-product | Drop / keep | **Keep** | Compat | High if drop | All |
| 8 First build | Isolation / extraction+parity | **Build 1 extraction+parity harness** | Route | Med | GO |
| 9 Formula freeze | Allow change / freeze | **Freeze** | Services | Critical | All |
| 10 Pricing freeze | Allow change / freeze | **Freeze** | CPP | Critical | All |
| 11 Schema policy | None / required | **None for B1** | — | — | B1 |
| 12 Component-only commercial | Slice1 / expand | Slice1 after B3 | Deferred FINISH/MOUNTING | Med | B3 |
| 13–16 A/B/C/D | Per prior pack | After B2–3 scoped | Prior gates | Med | B2–3 |
| 17 Snapshot parity | In B2 / B4 | **B4 + GO** | Boundary | Med | B4 |
| 18 Exec preview parity | In B3 / B4 | **B4 + GO** | Boundary | Med | B4 |

---

## 45–47. Exclusions / worklog / commit

Forbidden: app edits; formula/price changes; Pricing 7I; CostEngine rewrite; `/price`; freeze/order/tasks; **SVG Analyzer redesign/implementation in this alignment stage**; Logo/ACM/sold chips; parallel catalog; second Intake; broad redesign; dirty cleanup.  
**Preserve** nest2 SVG contribution in parity (do not drop layers/colors/geometry/roles).

Worklog: `docs/worklog/realignment/2026-07-17_intake_v6_golden_reference_modular_reproduction_alignment.md`  
Optional: `docs/architecture/INTAKE_V6_GOLDEN_REFERENCE_PARITY_CONTRACT.md`  
Commit: **NU**

---

## 48. Metodă

Runtime > code > payload > tests > audits (E2E gradi + modular + owner gates) > plans.  
Contradiction rule: if a plan calls golden “broken,” reject — extract ownership instead.  
SVG: Corel real export > nest2 FE analyzer > operator confirm > quote_geometry > PD/formulas — Analyzer never owns money or sold scope.

---

## 49. Roadmap awareness checkpoint

| Item | Value |
|------|--------|
| Position | After modular leakage audit + owner gates; **before** any rewrite |
| Why parity first | Protect validated formulas, prices, UX, E2E, **SVG facts** |
| Unlocks | Safe subset composition without greenwash; selective SVG consumption |
| Remains blocked | Sold FINISH/MOUNTING; packaging sold; Employee Mobile; freeze without GO; full LETTER/LOGO/ACM classifier |
| CPP protection | Exact parity harness; no tariff edits; SVG→qty only via frozen formulas |
| Snapshot/Exec | Compat tests; B4 separate GO |
| Score | **9/10** |
| Cat sunt in directia stabilita | **82/100%** (SVG+UI golden flow integrated) |

Canonical spine:

```text
GOLDEN SVG (Corel) + GOLDEN INTAKE V6
→ ownership extraction (incl. SVG→component mapping)
→ modular contracts
→ full-product reproduction
→ SVG + commercial parity proof
→ subset composition (selective SVG facts)
→ output isolation
→ CPP parity
→ frozen modular snapshot
→ Execution preview
```

---

## 50. Dead pieces check (no cleanup)

Hardcoded UI/defaults; global values without provenance; parent materials/ops always in raw Aggregate; adhesive misowned; legacy `active=[]`; soft defaults; inactive warnings; preview paths that skip filtered Aggregate; **visibility attrs not parsed**; **nested group path flattened**; **ACM aspirational on gradi-curat**; weak PD provenance from analyzer — inventory only.

---

## 51. Cat sunt in directia stabilita: 82/100%

---

## 52. Next safe step

Owner confirms: golden keep · parity-first · formula/price freeze · **SVG parity in Build 1 harness** · Build 1→4 route · interface adhesive model.  
Optional: agent-mode re-run of fresh disposable 15-step UI script (plan-mode blocked writing `.mjs` this turn).  
Then issue **one large implementation GO** for Build 1 only.  
Do not implement A–D, subset isolation, or SVG Analyzer redesign first.

---

# BINDING ADDENDUM — SVG ANALYZER + REAL UI GOLDEN FLOW

## SVG-1. Source workflow (Corel)

| Item | Truth |
|------|--------|
| Producer | P-Media internal CorelDRAW export |
| Reference file | `C:/Users/offic/Desktop/fisiere-teste-svg/gradi-curat.svg` |
| Nature | **Golden technical input** — not synthetic fixture |
| Size / hash (proven) | 27173 bytes · `593c4d43…6cf1` |
| Intent | Transport layers/groups/colors/geometry for system consumption |

## SVG-2. Analyzer boundary (law)

```text
SVG ANALYZER = detect + measure + propose technical interpretation
OPERATOR     = confirm or correct
PRODUCT SYSTEM = semantic authority over possible components
PRODUCTDEFINITION = concrete technical scope
PRODUCTAGGREGATE = technical outputs
CPP = price authority
```

Analyzer must **not**: decide final product/sold modules/active scope alone; compute commercial price; create CPP lines/tasks/ExecutionPlan; replace operator confirmation.

## SVG-3. Capability inventory (code truth)

Canonical path: FE nest2 `frontend/src/lib/svgAnalyzer` → `analyzeSvgString` → `PUT …/analysis-bundle` → `svg_analysis_json` + `quote_geometry` + `layer_role_setup` + composition recommendation. Backend does **not** re-run nest2.

| Capability | File/function (primary) | Persisted? | UI? | Downstream | Verdict |
|------------|-------------------------|------------|-----|------------|---------|
| Layers / pseudo layers | `parseSvg` + semantic expansion + `analyzeLayers` | Yes | Yes | roles, composition | `IMPLEMENTED_AND_USED` |
| Groups | `parseSvg.groups` | Partial | Partial | pseudo expansion | `IMPLEMENTED_PARTIAL` |
| Nested groups | Flattened layer boundaries | No path | Flat list | Over-split risk | `IMPLEMENTED_PARTIAL` |
| Element IDs | parse element ids | Partial | Preview by layer | parts | `IMPLEMENTED_PARTIAL` |
| Colors fill/stroke | `classifyPaint` / `analyzeColors` | Yes | Culori + role hints | heuristics | `IMPLEMENTED_AND_USED` |
| Visibility attr | — | No | No | May count hidden | `MISSING` |
| Transforms | geometry transformCount/warnings | Yes | Warnings | confidence | `IMPLEMENTED_PARTIAL` |
| Doc dims mm | normalize + analyzeGeometry | Yes → quote_geometry | Yes | PD geometry_inputs | `IMPLEMENTED_AND_USED` |
| Contours | closedSubPathCount | Yes | Contururi | letter parts | `IMPLEMENTED_AND_USED` |
| Perimeter / area | analyzeGeometry + quote_geometry | Yes | Yes / PD | formulas LETTER_* | `IMPLEMENTED_AND_USED` |
| Candidate LETTER | guessLayerAutoRole / name heuristics | Roles | Vector Litere | composition letters | `IMPLEMENTED_PARTIAL` |
| Candidate LOGO | artwork/logo heuristics | Roles | Vector Logo | LOGO_v1 composition | `IMPLEMENTED_PARTIAL` |
| Candidate ACM | name synonyms → support_panel | If confirmed | Limited dropdown | pending ACM template | `IMPLEMENTED_PARTIAL` — **not proven on gradi-curat** |
| VECTOR ATIPIC | composition label / logo_vector_atipic | Composition string | Not first-class chip | Logo path | `UI_NOT_EXPOSED` |
| UNKNOWN | autoRole unknown | Yes | De confirmat | blocks if none constructive | `IMPLEMENTED_AND_USED` |
| Confidence / warnings | layer + detectWarnings | Yes | Yes | readiness notes | `IMPLEMENTED_AND_USED` |
| Provenance chain to PD | weak | Partial | Weak | geometry without full chain | `CONSUMED_WITHOUT_PROVENANCE` |

Parallel legacy: `backend/services/svg_analyzer.py`, vector-assets — **LEGACY**, not nest2 SoT for this path.

## SVG-4. SVG → component / formula / price mapping

| SVG fact | Analyzer output | Component consumer | Formula | UI confirm |
|----------|-----------------|--------------------|---------|------------|
| Layer/group | layers[], ids | FACE/CANT/LOGO via roles | — | Yes (Straturi) |
| Color | colors / paintEvidence | finish hints / artwork | — | Review |
| Contour / perimeter | perimeterMl, letter_perimeter_m | CANT (+ FACE interface) | LETTER_PERIMETER | After role confirm |
| Area | face_area_m2 | FACE / finish | LETTER_FACE_AREA | After role confirm |
| Width/height | quote_geometry dims | Shared geometry | document | Auto |
| LETTER candidate | autoRole face | Letters composition | letter_count | **Required** |
| LOGO candidate | artwork/logo | Logo segment | — | **Required** |
| ACM candidate | support_panel | ACM template pending | — | Only if proven |
| Atypical / unknown | unknown / low confidence | Block or map | — | **Required** — do not force |
| Confidence/warnings | fields | readiness | — | Visible |

**Price:** Analyzer never prices. Qty → CPP via `quote_geometry` + commercial rules only.

## SVG-5. Modular SVG consumption (after full-product parity)

| Scenario | Consumes | Must not consume |
|----------|----------|------------------|
| CANT only | contours, perimeter, count, selected layers, return settings | face area materials, LED, back, mounting, template, **bonding adhesive** |
| FACE only | contours, area, colors, face settings | cant forming, adhesive, LED (unless lighting sold) |
| FACE+CANT | both + **interface bonding+adhesive** | — |
| LOGO only | logo layers/groups | Only if request mode allowed |
| Atypical | stays blocked / explicit map | No forced component |

## SVG-6. Golden SVG parity contract

```text
CURRENT GOLDEN INTAKE (same SVG + same operator choices)
=
MODULAR REPRODUCED INTAKE
```

| Layer | Parity requirement |
|-------|-------------------|
| SVG facts / layer identity / colors | exact |
| Geometry / quantities | exact (tolerance documented if any) |
| Candidate roles | equivalent |
| Operator approvals / components | equivalent |
| Formulas | exact |
| Materials / operations | equivalent |
| CPP lines / price totals | **exact** |
| Readiness / Review | functional parity |
| Persistence | no loss |
| Provenance | same or improved |

Must not lose: layer IDs, group hierarchy (available facts), colors, geometry, roles, operator confirmation, formula inputs, price inputs.

---

## SVG-7. UI golden walkthrough — mandatory evidence

### Route truth
| URL | Observed |
|-----|----------|
| `/intake-v6` | Catch-all → `/dashboard` (not yet redirected to operator) |
| `/intake-v6/operator` | Bootstrap → `/intake-v6/{id}/operator` |

### Evidence authority (same day, disposable)
| Item | Value |
|------|--------|
| Workspace | `4888fddb-5d9f-46cb-9bcc-5dd3ed1263b1` / `IV6-C8066690` |
| Template | `TPL-VOLUMETRIC-LETTERS_v2` |
| SVG | `gradi-curat.svg` via UI file input |
| Evidence dir | `docs/audits/_evidence/2026-07-17_intake_v6_gradi_curat/` |
| Audit | `docs/audits/2026-07-17_intake_v6_e2e_ui_functional_logic_gradi_curat_audit.md` |
| Fresh re-run this turn | **TOOLING_BLOCKED** (plan-mode rejected non-markdown walkthrough script; mode switch rejected) — prior evidence stands |

### Step ledger (proven from evidence)

| Step | Action | Evidence | Verdict |
|------|--------|----------|---------|
| 1 New request | `/intake-v6/operator` auto-creates WS | `01_*`, workspace id | PROVEN |
| 2 Select SVG | UI `intake-v6-svg-input` + Desktop file | `04_*`, `05_*` | PROVEN |
| 3 Analyze | analysis-bundle 200; 6 layers, 5 colors, 36 contours, 5087×600 mm | `06_svg_inspect.png`, evidence.json | PROVEN |
| 4 Approve layers | Confirm all suggestions; roles Vector Litere/Logo | phase2 + payload selected_layer_refs | PROVEN |
| 5 Approve components | Letters + Logo composition confirmed | phase2 composition_confirmed | PROVEN |
| 6 Form | Finisaje / Iluminare / Montaj | `22_tab_*.png` | PROVEN |
| 7 Live calc | Geometry + commercial preview spine | Review samples / dry-run | PARTIAL (blockers remain) |
| 8 Live changes | Illumination/template toggles documented in audits | conditional tests | PARTIAL |
| 9 Leave/return | dashboard → reopen | `09_*`, `11_*` | PROVEN |
| 10 Hard refresh | SVG still attached | `23_hard_refresh_layers.png` | PROVEN |
| 11 ProductDefinition | geometry_inputs.vector_file=gradi-curat.svg | phase2 / API | PROVEN |
| 12 ProductAggregate | template compile 200 | phase2 | PROVEN |
| 13 CPP dry-run | dry_run_only; Intake ≠ money | phase2 priced | PROVEN_BOUNDARY |
| 14 Review | ownership notes; blockers | `13_review_deep.png` | PROVEN |
| 15 Snapshot/Exec | handoff blocked; no freeze created | quote-handoff-preview | NOT_REACHED_SAFE |
| Responsive | 1440 / 1280 / 768 no h-scroll | `15_responsive_*` | PROVEN |

### Fact match (gradi-curat)

| Fact | Code/API | UI | Match |
|------|----------|-----|-------|
| Filename | svg_source.file_name | Fișier recunoscut | YES |
| Dims | ~5087×600 mm | 5087×600 mm | YES |
| Layers | 6 | Straturi 6 | YES |
| Colors | 5 | Culori 5 | YES |
| Contours | 36 | Contururi 36 | YES |
| LETTER/LOGO roles | vector_litere / vector_logo | Vector Litere / Vector Logo | PARTIAL (heuristic + confirm) |
| ACM | not on file | not shown as ACM product | ABSENT (correct) |
| Atypical | unknown path exists | De confirmat / Logo path | PARTIAL |

### Classification proof
- LETTER / LOGO: **PARTIAL** — operator confirmation drives letter_count / composition  
- ACM: **NOT PROVEN** on gradi-curat — do not declare detected  
- Atypical: do not force classification for readiness  

### Operator confirmation boundary
Proposed roles ≠ final. Unconfirmed layers block composition/progress. Analyzer proposal + operator confirm = golden law.

---

## SVG-8. Owner gates (SVG-specific)

| Gate | Question | Blocks which build? |
|------|----------|---------------------|
| SVG-G1 | Nest2 remains SoT for Intake V6 Letters? | B1 if changed |
| SVG-G2 | LETTER/LOGO classifier depth vs keep PARTIAL + confirm? | B2–3 |
| SVG-G3 | ACM detection when (name vs geometry)? | Later — not gradi |
| SVG-G4 | Must visibility/group_path be preserved for parity? | B1 harness |
| SVG-G5 | Fresh 15-step disposable re-run required before Build 1 GO? | Owner choice |

---

## SVG-9. Risks

- Flattened nested groups lose hierarchy  
- Hidden geometry may still measure (`visibility` MISSING)  
- Weak provenance to PD/formulas  
- False LETTER/LOGO/ACM classification if forced  
- Modular subset reusing face area for cant-only  
- CPP qty divergence if quote_geometry rebuilt differently  

---

## 53. STOP

No implementation. No commit.  
Alignment now includes **SVG golden input + Analyzer boundary + UI walkthrough evidence + SVG parity contract**.  
Await owner review.

