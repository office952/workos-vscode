# Console / network (U7 capture)

Runtime: FE `:3043` · capture `u7-capture-results.json`

| Finding | Class |
|---------|-------|
| ProductSystem preview 404 on `/execution/:id` | PREEXISTING (not introduced by AppShell) |
| Shop Floor live-data unavailable warning | PLATFORM / honest empty — not fabricated |
| React Router future-flag warnings in unit tests | PREEXISTING dep noise |

No redirect loops observed. Operator `/inventory/pricing` → redirect to `/shop-floor` (UI guard).
