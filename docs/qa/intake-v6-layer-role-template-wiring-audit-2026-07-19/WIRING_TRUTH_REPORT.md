# INTAKE V6 LAYER ROLE AND TEMPLATE WIRING TRUTH REPORT

## 1. Verdict
**FAIL for next UI polish** — wiring not first-paint truthful. Letter visual pilot remains accepted separately.

## 2. Mini decizia agentului
Document proposal misclassification + confirm Contur suport path; do not implement; do not extend DS polish.

## 3. Git state
`feature/product-system-active-path-isolation-v1` @ `f39c260`; foreign WIP untouched.

## 4. Runtime and served commit
FE `:3000` + BE `:8003`; HEAD `f39c260`. `:3001` crash operational only.

## 5. Fixture inventory
Desktop fixtures listed in `runtime-evidence.md` (ACM segmented, crossing, situatie-3, simple letters).

## 6. SVG detection trace
Client: 2× `pseudo:fill-*`. Server named layers with `unknown`. Dual path gap.

## 7. Role proposal trace
Both ACM grey and letters → **Vector Litere** (`guessLayerAutoRole` pseudo→face).

## 8. Operator confirmation trace
Known wired: grey → `support_panel`, red → `face`; persisted.

## 9. Navigation behavior
Role select: no step jump. Progress: confirm accessible with review once analysis ready.

## 10. Component binding trace
face→FACE_v1 LETTER_VECTOR_SET; support_panel+contour→ACM SUPPORT_CONTOUR.

## 11. `TPL-VOLUMETRIC-FACE_v1`
Code-owned contract constant; attaches when role confirmed `face`.

## 12. ACM shell template wiring
Registered/selectable/PD/dry-run/Montaj proven after confirmation; association save can fail on fresh client upload.

## 13. ProductDefinition evidence
Letters + ACM PD 200 on wired workspace.

## 14. ProductAggregate evidence
production-task-dry-run 200 (preview).

## 15. Page 2 rendering truth
Montaj hosts ACM; Finisaje hosts letter finishes; incomplete roles block clusters.

## 16. Segmented background truth
CONFIRMED on wired workspace under FinishSetup.

## 17. Hardcoded/fallback findings
FACE/ACM contract constants active; BOND-CASETAT legacy; pseudo→face dangerous.

## 18. Dead pieces check
Legacy Face/Cant sections unused in ReviewStep — not revived.

## 19. Runtime screenshots
`screenshots/` + `runtime-evidence.md`.

## 20. Root cause classification
Primarily analyzer proposal + role confirmation (classes 1–2), with ingest dual-path (7).

## 21. Smallest correct repair boundary
Pseudo-layer support proposal + Contur suport persistence reliability (+ optional confirm step gate).

## 22. Areas that must remain frozen
Montaj IA redesign, pricing, visual DS expansion, domain compilers without GO.

## 23. Tests/evidence
Runtime JSON/screenshots; static contract tests already cover bindables.

## 24. Files modified
QA pack + worklog + letter-pilot runtime closure docs.

## 25. Files not modified
FE/BE/analyzer/schemas/templates/DB.

## 26. Worklog
`docs/worklog/realignment/2026-07-19_intake_v6_layer_role_template_wiring_audit.md`

## 27. Commit
`docs(intake-v6): audit layer role and template wiring`

## 28. Metoda de lucru
Pre-flight → contract map → server+client runtime walks → known wired contrast → docs-only commit.

## 29. Roadmap awareness checkpoint
Nota 8/10 audit discipline; blocks polish; unlocks targeted wiring repair; Employee Mobile out of lane.

## 30. Cat sunt in directia stabilita
Cat sunt in directia stabilita: **93/100%** (owner trajectory; this audit protects it)

## 31. Can the next UI build proceed safely?
**NU** — ACM proposed as Vector Litere; Contur suport must be operator-corrected; dual ingest gap.

## 32. Next recommended build
Owner GO only: **Analyzer + Page 1 Contur suport proposal/persistence truth repair** (no Montaj redesign, no DS polish).
