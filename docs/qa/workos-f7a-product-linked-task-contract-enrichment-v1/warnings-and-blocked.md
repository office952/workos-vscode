# F7A — Warnings and blocked items

## Allowed PASS WITH WARNINGS

| Warning | Why acceptable |
|---------|----------------|
| `PLANNING_MINUTES_SOURCE_REQUIRED` / `estimated_minutes=null` | DEC-006 explicit; scheduling production-ready remains blocked |
| `DAG_PROCESS_DEPENDENCIES_UNRESOLVED` when no process/catalog edges | Honest; no invented linear order |
| Premount remains BOM-only (no activation signal in repo) | DEC-002; finding, not invented boolean |
| ProductSystem 404 (U7 preexisting) | Not reproduced this gate; FE untouched |
| Families outside volumetric pilot | Not claimed universal |

## Still blocked (by design)

```text
POST materialize (DEC-009 = A)
operational_tasks writes
sessions / assignment / Employee Mobile
Pricing / commercial rewrite
SVG/DWG processing
F7B materialize pilot (needs Owner DEC-009 = B after F7A review)
```

## No owner decision required for F7A close

Alias parents, premount default, minutes null, and DAG non-linear policy are Owner-bound and implemented.

Future Owner decisions (out of F7A):

1. Premount activation signal if/when manufacturing defines one.
2. DEC-009 = B only after independent F7A review PASS.
3. Distinct multi-finish painting collapse rules if a product proves two physical paint ops.
