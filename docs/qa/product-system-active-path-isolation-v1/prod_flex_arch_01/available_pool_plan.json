{
  "task": "PROD-FLEX-ARCH-01",
  "current_debt": "_has_active_session_by_other in list_available_tasks hides task from all eligible employees",
  "remediation": "SPLIT_POOLS — do not delete guard from principal pool without replacement",
  "pools": {
    "taskurile_mele": {
      "includes": ["assigned_employee_id=self", "claimed self", "active HELPER participation", "active PRINCIPAL session"],
      "api_projection": "mobile truth assigned + in_progress sections"
    },
    "disponibile_pentru_principal_claim": {
      "includes": ["READY", "eligible", "no other principal assignee OR self-claimable", "no blocking deps"],
      "excludes": ["done", "foreign principal assignee"],
      "keeps": "existing claim concurrency guard"
    },
    "ajutor_solicitat": {
      "includes": ["task in_progress", "OPEN help request", "eligible for HELPER"],
      "excludes": ["assignee required for view"],
      "bypasses": "_has_active_session_by_other for helpers"
    },
    "in_lucru_in_zona_mea": {
      "includes": ["same workcenter read-only"],
      "implies_eligibility": false
    }
  },
  "key_separation": "available_for_principal_claim != available_for_helper_join"
}
