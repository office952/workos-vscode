# Policy contract

Exact role+skill match, then role-only; no match is unavailable. Intervals are effective-dated and active intervals cannot overlap. Policy boundary-crossing sessions are unsupported, never silently split.
