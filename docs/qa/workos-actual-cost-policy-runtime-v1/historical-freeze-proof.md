# Historical freeze proof

Pre-V1 sessions lack explicit `actual_cost_policy_runtime_v1` eligibility and are not valued. Existing order 973019 is reported as `historical_policy_unavailable` / `historical_cost_not_frozen`; no backfill occurs.

