# Wave 3 legacy/compat

Source: `lane-g-legacy-dead/CLASSIFICATION.md`.

LEGACY_REMOVE_CANDIDATE_COUNT = **0**

`/operator` and `/tablet` are **COMPAT_ACTIVE** (live). `/employee-app-v2` is **SPECIALIZED_EMPLOYEE_UI**, hidden from shell. Ops-Graph **AUDIT_ONLY** (assign picker present, not used). Atelier is **CANONICAL_MONITOR_HOME**. See `WAVE_3_ACTION_SURFACE_CLASSIFICATION.md`.
