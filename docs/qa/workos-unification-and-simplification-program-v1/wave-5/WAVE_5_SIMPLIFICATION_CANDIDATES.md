# Wave 5 — simplification candidates (audit only)

1. Keep `/employees` as the only master; mark Evidență HR as DEMO or bind it to real files.
2. One supplier **UI** (Inventory **or** Colaboratori) — same API already; not two registries (`SAME_TRUTH_DIFFERENT_PROJECTION`).
3. Do not treat `/documents` as a live module until a document store exists.
4. Move Plăți/Avansuri under Oameni or say Management is money.
5. Link Utilaje → MachineRun as “stare execuție”, not occupancy on the catalog.
6. Label `/reports` KPIs as model/projection (OTIF defaults).
7. Keep Pricing as registry; do not make it the offer editor.
8. Do not merge attendance into payments writes.
9. Settings company mock: hide or wire.
10. Orphan MaterialPriceRegistry file — later, not now.

No implementation now.
