# Evidence — Volumetric Letters authority / instance / placement (Build 1)

| Artifact | Purpose |
|----------|---------|
| `runtime_proof.py` | Dry coalesce + quantity builder + CPP measurement dry-run on real SQLite fixtures |
| `runtime-proof.json` | Captured output (no finish PUT / Offer / Order / Execution writes) |

Fixtures used: `IV6-195E885C` (4-group gradi-curat), `IV6-DB2F86B7`, `IV6-13D39D32`.

No operator UI screenshots — UI not visually redesigned.
