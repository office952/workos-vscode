# Legacy TPL-ACP-LIGHT-ROUTED — Adapter Plan (future)

**Current status:** `PARALLEL_LEGACY_COST_PATH`  
**Not** Intake V6 / Product System authority for composable face modules.

## Allowed use now

- Discover historical terms (diffuser, relief plexi, LED cavity wording)
- Compare intent vs live ACM boxed shell
- Detect fork / dual-path consumers

## Forbidden until separate owner GO

- Auto-migrate workspaces
- Import task_rules / pricing formulas into V6
- Replace live shell `TPL-ACM-BOXED-MOUNTING-SUPPORT_v1`
- Treat CostEngine seed as Resource Options catalog

## Future adapter (outline only)

1. Map LIGHT-ROUTED fields → local module intents (identity only)
2. Explicit owner confirmation per mapped value
3. Dual-run / dry-run projection before cutover
4. Retire cost path only after Aggregate + CPP readiness
