# Intake V6 Golden Ownership Map (Build 1)

**Contract version:** `intake_v6_golden_ownership_v1`  
**Machine-readable twin:** [`backend/tests/fixtures/intake_v6_golden_gradi/ownership_map.json`](../../../backend/tests/fixtures/intake_v6_golden_gradi/ownership_map.json)  
**Parity contract:** [`docs/architecture/INTAKE_V6_GOLDEN_REFERENCE_PARITY_CONTRACT.md`](../INTAKE_V6_GOLDEN_REFERENCE_PARITY_CONTRACT.md)

## Law

```text
SVG ANALYZER = detect + measure + propose
OPERATOR     = confirm / correct
PRODUCT SYSTEM = semantic authority for possible components
PRODUCTDEFINITION = concrete technical scope
PRODUCTAGGREGATE = technical outputs
CPP = money authority
```

Build 1 **labels** ownership; it does **not** move runtime ownership when output would change.

## Interface candidate (documented, not activated)

```text
FACE + CANT → RETURN_FACE_BONDING + MAT-ADEZIV-CANT-LITERE
current_owner = modelare_cant
target_owner  = interface:FACE+CANT
Build 3       = cant-only silence
```

Live Aggregate on historical golden WS **does** emit adhesive + bonding.  
`volumetric_v2_db` pytest seed is thinner and does **not** emit adhesive — documented in aggregate fingerprint.

## Settings provenance (Build 1)

Additive metadata only — no schema / migration:

```text
owner · source · confirmed · contract_version · consumers
```

## Full element table

See `ownership_map.json` for the executable inventory (SVG identity, layers, colors, geometry, materials, LED/PSU, template, mounting, packaging, adhesive, CPP inputs, readiness, LETTER/LOGO/ACM/atypical).
