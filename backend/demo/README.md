# WorkOS Atoms demo DB

Generated file (gitignored):

- `workos_demo.db`

Never use or overwrite `../dev.db`.

## Bootstrap / start

From repo root (Windows PowerShell):

```powershell
.\scripts\demo-bootstrap.ps1
.\scripts\demo-start.ps1
```

See `docs/operations/WORKOS_ATOMS_DEMO_ENVIRONMENT.md`.
