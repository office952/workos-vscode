# Intake V3 test fixtures

Committed SVG/JSON fixtures for pytest operator workspace E2E hardening (Phase 6).

| File | Scenario |
|------|----------|
| `multi_layer_ten_layers.svg` | 10 productive layers + reference + inner hole layers |

Do not commit Atoms HTML exports from `tmp/`.
